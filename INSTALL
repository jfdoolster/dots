#!/bin/bash

SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

cp -r $SCRIPT_DIR/.vim $HOME
cp $SCRIPT_DIR/.bash_profile $HOME
cp $SCRIPT_DIR/.bash_aliases $HOME
cp $SCRIPT_DIR/.bash_ssh $HOME
cp $SCRIPT_DIR/.bash_logout $HOME